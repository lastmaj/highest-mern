{
  "name": "back",
  "version": "1.0.0",
  "description": "",
  "main": "src/server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node start --prefix src",
    "client": "npm start --prefix client",
    "dev": "set NODE_ENV=production&&npm run dev --prefix src",
    "heroku-postbuild": "npm install --prefix client && npm install --prefix src && npm run build --prefix client"
  },
  "author": "maj",
  "license": "ISC"
}